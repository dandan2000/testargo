apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "oper-installer.fullname" . }}-test-connection"
  labels:
    {{- include "oper-installer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "oper-installer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
